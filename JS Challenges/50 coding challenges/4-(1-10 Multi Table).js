for (let index = 1; index <=10; index++) {;
    for (let i = 1; i <= 10 ; i++) {
        console.log(`${index} x ${i} = ${index*i}`);
        document.write(`${index} x ${i} = ${index*i}<br>`);
    }
}