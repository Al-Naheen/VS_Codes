//program for adding two numbers.
/'program for adding two numbers.'/
#include<stdio.h>
main()
{
    float x, y, ;
    scanf("%f",&x);
    scanf("%f",&y);
    sum=x+y;
    printf("Sum is:%f", sum);
    return 0;
}
